server:
  port: 8081 # ?? ??, ??? 8081 ??? ??

spring:
  application:
    name: joycrew

  profiles:
    active: dev # ??? H2 ?? ??

logging:
  level:
    org.hibernate.SQL: debug      # ?? ??
    org.hibernate.type.descriptor.sql.BasicBinder: trace # ??? ?